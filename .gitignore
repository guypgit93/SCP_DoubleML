# ── FRS data — never commit (restricted access via UK Data Service EUL) ──────
data/
*.tab
*.csv

# ── Superseded/redundant scripts — kept locally, not tracked (2026-07-20) ────
03c_agecohort_stacked_item_did.R
04_dml_did.R
04b_chang_dmldid_trial.R
04c_agecohort_staggered_did.R
04d_propensity_overlap.R

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
*.swp
.idea/
.vscode/

# ── Non-script project files — repo is scripts-only (2026-07-20) ────────────
# Kept on disk, just not tracked. Add explicit exceptions above this block
# with `!filename` if a specific one should be committed later.
*.docx
*.pptx
*.ppt
*.pdf
*.xlsx
*.xls
*.png
*.jpg
*.jpeg
*.md
*.txt
*.bib
config.py
Papers/
figures/
tables/
latex/
archive/
~$*
testfile_delete_me.txt

# ── Exceptions to the non-script rules above ──────────────────────────────
!README.md
